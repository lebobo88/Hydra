{
  "mcpServers": {
    "pp-daemon": {
      "command": "node",
      "args": ["C:/AiAppDeployments/pair-programmer/daemon/dist/index.js", "mcp"],
      "env": {
        "PP_HARNESS_MCP": "1"
      },
      "_comment": "Engineering squad. Wired to the pair-programmer harness daemon."
    },
    "hydra-memory": {
      "command": "python",
      "args": ["-m", "mcp_servers.hydra_memory"],
      "cwd": "${CLAUDE_PLUGIN_ROOT}",
      "env": {
        "HYDRA_EPISODIC_DB": "${USERPROFILE}/.hydra/episodic.db",
        "HYDRA_VECTOR_DIR": "${USERPROFILE}/.hydra/vectors"
      },
      "_comment": "Hydra memory fabric — episodic + semantic store as MCP resources."
    },
    "executive-suite": {
      "command": "python",
      "args": ["-m", "mcp_servers.executive_suite"],
      "cwd": "${CLAUDE_PLUGIN_ROOT}",
      "env": {
        "HYDRA_ES_ROOT": "C:/AiAppDeployments/ExecutiveSuite"
      },
      "_comment": "Executive squad — thin MCP over the ExecutiveSuite pack (roster, skills, commands, output persistence)."
    },
    "rlm-creative": {
      "command": "python",
      "args": ["-m", "mcp_servers.rlm_creative"],
      "cwd": "${CLAUDE_PLUGIN_ROOT}",
      "env": {
        "HYDRA_RLM_ROOT": "C:/AiAppDeployments/RLM-Creative"
      },
      "_comment": "Garland Crown — Eight Garland Heads studio. Thin MCP over RLM-Creative (skills, commands, agents, output persistence). Repointed from RLM-CLI-Starter on 2026-05-19 when the Garland Crown source pack shipped."
    }
  }
}
