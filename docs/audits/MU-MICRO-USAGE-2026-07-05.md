# MU — Hydra Micro-Usage Verification Campaign — 2026-07-05

Branch: `fable-audit-2` (HEAD 45dc757). Plan: `~/.claude/plans/ever-since-all-the-logical-lark.md`.
Policy: fix-as-found; Tiers 0–4 (full coverage, ~$15-35 ceiling).

## Findings ledger

| ID | Sev | Tier/Cell | Feature | Invocation | Expected | Observed | Evidence | Suspect commit/module | Status |
|----|-----|-----------|---------|------------|----------|----------|----------|-----------------------|--------|
| MU1 | S2 | T0/0.3 | `hydra doctor` full-mode MCP probe | `python -m hydra_core.cli doctor` | pp_harness + hydra_memory reachable | WARN "pp_harness unreachable — unknown tool: ping" + "hydra_memory unreachable — unknown tool: list_tools"; both servers respond fine via gateway (pp_harness doctor OK, hydra-mem_list_workflow OK) | doctor stdout 2026-07-05; gateway probes same session | `hydra_core/cli.py` `_cmd_doctor` probes nonexistent tool names → false negatives | open |
| MU2 | S1 | T0/0.17 | Enforcement hooks (Phase 5 headline) | synthetic PreToolUse stdin → block hooks | write-idiom → exit 2 block | Hook exited 0 silently: **persistent User-level env var `HYDRA_PP_STAGE_ACTIVE=1`** (leaked debug artifact, likely 2026-06-23 harvest session) bypassed ALL enforcement hooks machine-wide since ~06-23; settings.json itself correct | `[Environment]::GetEnvironmentVariable('HYDRA_PP_STAGE_ACTIVE','User')` = 1; hook logic verified correct (exit 2 block on .py, allow git/.md/worktree) once var stripped | env leak, not code — `hydra-block-bash-writes.ps1`/`hydra-block-direct-write.ps1` gated on it | **fixed** (User var removed; current session still inherits it — re-verify hooks next session) |
| MU3 | S3 | T0/0.6 | Checkpoint deserialization | `hydra status` / `budget` / `reap` | clean output | langgraph warning: "Deserializing unregistered type hydra_core.state.TaskState / BudgetLedger ... will be blocked in a future version" — future-version time bomb | stderr on every checkpoint read | `hydra_core/state.py` types not registered in allowed_msgpack_modules | open |
| MU4 | S3 | T0/0.18 | Workflow store hygiene | `hydra reap` (dry-run) | small candidate list | 272 of 500 scanned workflows are stale non-terminal (some 617h old); status scan capped at 500 | reap dry-run output | accumulation, no scheduled reap | open |
| MU5 | S2 | T0-lead | Repo targeting at launch | wf `03eeadc6` (launched 14:33Z, goal says "Repo: pi-pp-platform monorepo" in prose) | dispatch lands in pi-pp-platform (registered in `~/.hydra/repos.json`, exists on disk) | `workflow_start` carried NO target_repo_id → pp run `project_path=C:\AiAppDeployments\Hydra`; all 3 best-of candidates wrote honest wrong-repo blocker reports; codex judges failed all 3; stage surfaced; **$3.72 burned on a run that could never succeed**; zero warning that goal-text repo ≠ dispatch repo | pp `get_run run_fTRQTRWKyi_-`; trace.jsonl workflow_start; repos.json | launcher didn't pass `--repo`; no intake cross-check of goal-text repo mentions vs registry | open |
| MU5b | S3 | T0-lead | Hydra→pp provenance stamping | same run | pp run rows carry `hydra_workflow_id`/`hydra_envelope_id`/`hydra_origin_squad` | all null despite the run being Hydra-dispatched (trace links exist only Hydra-side) | pp `get_run` output | `squad_node._via_mcp`/`start_run` args | open |
| MU6 | S2 | T1 | Smoke gate reliability in Hydra repo | smoke = full `python -m pytest -q` (1319 tests, 3-10 min) per best-of candidate | deterministic verdicts | Same interpreter (Py 3.14.0), same tree: controlled Tier-1 run = **1318 passed/1 skipped/176s**; smoke runs during candidate generation = 5 failed/4 skipped/590s+366s and candidate 3 **timed out at 600s → infra_error**. Suite contains wall-clock-sensitive tests (test_dispatcher_timeout, test_p2_timeouts, test_gateway_timeout) that flake under load; full-suite-as-smoke × N candidates is 15-30 min of smoke per stage and self-DoSes the box the generators run on | Tier-1 output (green); pp run smoke_results | `_run_smoke`/`_detect_smoke_command` (squad_node.py:383/:340) — no scoped/quick smoke profile, 600s cap vs 590s baseline duration | open |
| MU6b | S3 | T1 | Smoke forensics | smoke_results retention | full output artifact for failed smoke | only the LAST line of stderr/stdout is kept (`tail[0]`, 300 chars) — the 5 failing test ids are unrecoverable after the fact | squad_node.py:431 | `_run_smoke` | open |
| MU8 | **S1** | T2/2.2 | **Attended MCP surface dead: plan/step/submit all time out** | `hydra.workflow.plan` (×6 probes, single + parallel) | plan JSON in ~2s (CLI parity proven) | Every call → `plan_timeout 180s`. py-spy native dump of hung child (pid 50368, CPU 0.015s): frozen in `Py_InitializeFromConfig → lseek/fflush(stdio) → NtQueryInformationFile` on the inherited stdin — the hydra_control server's stdin is a SYNCHRONOUS gateway pipe with the server's own read always pending; Windows blocks any same-handle operation → child never starts. `_run_cli_json` (server.py:311) omits `stdin=subprocess.DEVNULL`; `_launch_run` (detached, :296) passes it — hence detached works, attended never did through the gateway on Windows. With HYDRA_HOST_DRIVEN=1 default, ALL attended engineering is dead → **primary "hydra is broken" root cause** | wf 4c380c71/02f81593/75d45ca1/6cf5598c/c12a877c/795025e4/6028e782 (no trace dirs); py-spy dumps; env/stdin-pipe reproductions all pass in 2s | `mcp_servers/hydra_control/server.py:_run_cli_json` (attended surface added feat/attended-host-bridged-mode, hardened this branch) | **fixed** (user-approved bootstrap edit: `stdin=subprocess.DEVNULL` in `_run_cli_json`; both hydra_control servers restarted; probe L2 plans in ~2s with correct engineering routing; regression test to be back-filled via /hydra:run) |
| MU9a | S2 | T2/2.4 | Router selects stub squads | plan goal "triage patient intake symptoms…" | stubs never in dispatch set | `selected_squads=["healthcare","marketing-production"]` — healthcare is a stub; no warning that it cannot execute | wf ce4d31b8 plan output | `hydra_core/router.py` (no active-check) | open |
| MU9b | S3 | T2/2.4 | Keyword siphon | same clinical goal | single-squad routing | marketing-production co-selected on a goal with no marketing content | wf ce4d31b8 | router keywords | open |
| MU10 | S2 | T2/2.8 | `hydra.envelope.record` cannot record typed envelopes | valid-shaped HANDOFF + DECISION_RECORD probes | accepted | Phase 3a anti-shadow fix (payload never merged) removed the ONLY transport for type-specific REQUIRED fields (`Handoff.payload_envelope_id`, `DecisionRecord.decision/rationale`) → every typed envelope fails validation; AgentSmith HydraBridge.envelopeRecord likewise broken | tool errors this session | `mcp_servers/hydra_control/server.py:envelope_record` (b904fba round-2 fix) | open |
| MU12 | S2 | T3/3A | Attended surfaced runs destroy judge-passed work | cycle-1: judge pass + smoke skipped → surfaced | work preserved for operator pickup | worktree deleted on surface; branch `attended/run_NbM5tzbl0mfC` does NOT contain the engineer's untracked file (never committed pre-teardown); only artifact text in pp db survives. Repos with no test command can NEVER land attended work AND lose it silently | mc-test branch inspection | `host_bridge.py` teardown path (commit-before-teardown missing on non-complete) | open |
| MU6-rc | S1 | T3 | Smoke pytest runs under the hydra_control server's MINIMAL env | attended finalize smoke for run_f_xUsd4p5vSO | suite green (green in dev shell) | `5 failed, 1313 passed, 4 skipped in 568s` — identical 5F/4S signature as the morning run; smoke subprocess inherits the backend's 14-var env → 5 env-sensitive tests fail deterministically + 3.2× slower; ANY attended run on the Hydra repo can never finalize complete | cursor smoke_reason; minimal-env repro in flight | env propagation through `_run_cli_json`→submit→`_run_smoke` | investigating |
| MU12-b | S1 | T3 | MU12 recurrence on real work | MU7-fix run judge-passed, smoke-failed → surfaced | work preserved | branch `attended/run_f_xUsd4p5vSO` exists but points at bare 45dc757 (empty diff) — engineer's cli.py fix + 2 test files DESTROYED at teardown; recovered only because the full unified diff was embedded in the judge artifact | git show/diff | `host_bridge.py` (no commit-to-branch before teardown on non-complete) | open — elevates MU12 to S1 |
| MU14 | S3 | T3/3B | Timed-out generations record $0 cost | detached best-of run_wjf3gp9E5s5J | real API spend recorded | 3× 600s claude CLI timeouts → stage cost_usd 0.0 (spend invisible to budget ledger) | best_of.stage_outcome event | cost accounting on TimeoutExpired | open |
| MU16 | S2 | T4/4B | Fleet repo budget is accounting, not enforcement | fleet wf 60775c00, $4 split $2/$2 | leg halts at its repo allocation | mc-test leg spent $2.63 vs $2.00 allocation; workflow total $4.65 vs $4.00 (116%) — charges land after headless gen completes, no mid-leg stop | fleet budget output | `charge_and_gate_repo` timing vs headless gen | open |
| MU15 | S2 | T4/4A | Attended completions invisible to graph task ledger | campaign 9b7b835e: all 3 legs complete via attended seam, then resume through synthesis | postcheck reaches phase "done" | graph task states still `deferred_to_host`/`surfaced` (attended layer records completions in cursors/`attended_completed_task_ids`, never patches task.status) → governance honestly surfaces the finished campaign; synthesis/judge_synthesis DID run and merged the DECISION_RECORD | status output + synthesis_merge trace event | seam between host_bridge completions and HydraState.tasks | open |
| MU13 | S3 | T3/3A | Merge-back fragile to build byproducts | cycle-2: judge+smoke pass → pass_unlanded | merge succeeds | harness auto-committed `tests/__pycache__/*.pyc` into merge branch; harness smoke polluted main's working tree with same untracked pyc → merge abort. Honest `pass_unlanded` downgrade ✓ | wf 955c8575 submit result | `host_bridge.py` `_merge_worktree_back` (no ignore-filter/clean) | open |
| MU11 | S3 | T2/2.16 | Memory tag silent no-op | `hydra memory tag <unknown-key> --cells li` | error for nonexistent key | returns `{cells: []}` silently | CLI output | `hydra_core/memory.py` tag path | open |
| MU8b | S3 | T2/2.2 | Timeout forensics | `_run_cli_json` TimeoutExpired handler | partial stdout/stderr in error detail | partial output discarded on timeout (TimeoutExpired.stdout/.stderr unused) | server.py:324 | same | open |
| MU7 | **S1** | T2/2.1 | **Workflow lifecycle: synthesis unresumable** | keystone smoke wf `b6314c18`: run → approve (→dispatch→judge OK) → approve again | traversal to postcheck/terminal | Graph pauses at `interrupt_before=["synthesis","judge_synthesis"]` (supervisor.py:3054) but NO node files `pending_hitl` for these bare interrupts (only approval/budget/reflexion gates do); `_cmd_resume` (cli.py:692) refuses to `invoke(None)` when `pending_hitl` empty → **every standard workflow permanently parks at phase=synthesis; all resume actions are no-ops**. Explains the 272-workflow graveyard (oldest 617h ≈ 2026-06-10) incl. today's `03eeadc6`. Introduced by 7aa77e8 (mesh-console C2, 2026-06-05, pre-branch — on main too). Ingest is a separate seam and does not rescue run/approve | wf b6314c18 stuck (approve → `no_pending_gate`, phase synthesis, task done, judge passed); reap candidate list | `hydra_core/cli.py:_cmd_resume_locked` + `supervisor.py` interrupt list | open — **fix routed as Tier 3A attended engineering run** |

## Tier results

### Tier 0 — Read-only health sweep
**COMPLETE — Gate G0 PASS.** 18/18 cells executed.
- 0.1 baseline PASS (clean tree, HEAD 45dc757) · 0.2 doctor --quick PASS · 0.3 doctor full exit 0 but → MU1 · 0.4 verify PASS (sha 4060cb…cfa5, refusals 10) · 0.5 squads PASS (14 packs, model_tier revert intact) · 0.6 status PASS (→ MU3 warning) · 0.7 trace PASS · 0.8 budget PASS (caps recorded: defaults 50.0, one 25.0, one 10.0 exhausted) · 0.9 gateway_health PASS (16 backends, 0 failed; connections lazy) · 0.10 ping PASS · 0.11 squad_list PASS (CLI parity; stubs active:false) · 0.12 telemetry_tail PASS (matches trace) · 0.13 cockpit_audit PASS ({ok, spooled:true}) · 0.14 venom_cross_check PASS (fail-open rationale for unknown cap) · 0.15 memory PASS · 0.16 route-directive hook 652 ms incl. pwsh spawn PASS (9f43241 regression gone) · 0.17 block hooks → MU2 (S1, fixed) then verified correct · 0.18 reap dry-run PASS (→ MU4).
- **Bonus:** cross-vendor judge plane fully ready (codex gpt-5.4, gemini-3.1-pro-preview logged in + enabled, claude in-process) — stale "gemini dead" note resolved.
- **Lead for Tier 3:** wf `03eeadc6` run `run_fTRQTRWKyi_-` (15:08Z): best-of-3, ALL candidates judge-fail with `smoke: "infra_error"`, stage `surfaced`, winner merge `skipped`, cost $3.72 — matches the user's "broken" experience; investigate smoke-infra path before/at Tier 3.

### Tier 1 — Offline pytest suite
**COMPLETE — Gate G1 PASS.** `python -m pytest tests\ -q -rs` (Python 3.14.0): **1318 passed, 1 skipped** (rlm_creative not registered at user scope) in 176.54s. No unexpected skips; dispatcher/supervisor/hook coverage all executed. The 5 failures seen by the smoke gate at 14:47Z are NOT reproducible in a controlled run → reclassified as smoke-gate load sensitivity (MU6/MU6b).

### Tier 2 — Cheap live surface probes
**COMPLETE — Gate G2 PASS after MU8 fix.** Highlights:
- 2.1 keystone smoke lifecycle: run→approval pause→approve→dispatch(stub best-of-3 + Borda)→judge → **parked at synthesis = MU7 (S1)**. Graph + F9 gates + budget ledger all work up to the synthesis wall.
- 2.2/2.3 plan MCP+CLI parity: **MU8 (S1) found & FIXED** — attended MCP surface (plan/step/submit) was 100% dead through the gateway on Windows (stdin pipe inheritance freezes child at Py_Initialize); one-line `stdin=DEVNULL` fix, user-approved bootstrap edit, verified ~2s post-fix.
- 2.4/2.5 routing: engineering ✓ (probe L2), creative → garland+marketing-production ✓; **MU9a**: healthcare goal routed to the healthcare STUB as executable; **MU9b**: marketing-production keyword-siphoned onto an unrelated clinical goal.
- 2.6 attended step: full contract verified — worktree isolation, cursor await_generate→await_judge→surfaced, judge routing (same-vendor for zero-diff), honest `discarded_non_complete` merge, worktree auto-removed, $0 charged.
- 2.7 budget roundtrip ✓ (read 1.0 → set 0.5 → verify; capability_degraded per run-A posture). 2.8 envelope validation fail-closed ✓ but **MU10**: no typed envelope can pass (see ledger). 2.9 negative submit → clean cursor_not_found ✓. 2.10 wrong-state approve → no_pending_gate (=MU7 evidence). 2.11 reject positive → surfaced ✓. 2.12/2.13 live hook blocks NOT testable this session (inherited MU2 bypass; statically verified T0.17 with clean env — re-verify next session). 2.16 memory query-by-cell ✓; tag quirk → MU11. 2.17 triad consistency ✓.

**Tier-2 workflow ids (cleanup ledger):** b6314c18 (smoke, stuck@synthesis=MU7 evidence), 795aa5f9 (plan C, rejected✓), a2f95c8b (attended probe, surfaced✓, budget restored-to-0.5 note: was 1.0→set 0.5 test), 0a7887a9 + ce4d31b8 (routing probes, paused@approval — reject in cleanup), 4c380c71/02f81593/75d45ca1/6cf5598c/c12a877c/795025e4/6028e782 (MU8 timeout probes, no state).

### Tier 3 — Live micro-runs
**3A attended centerpiece: PASS (3 cycles, findings MU12/MU13 en route).**
- Cycle 1 (wf a70fd251): repo targeting via `--repo mc-test` works end-to-end (worktree in target repo) after `git init` of the scratch repo (step correctly fail-louds on non-git target). Judge PASS but smoke `skipped` (no test cmd) → PP-VG-5 refused complete → **MU12: judge-passed work destroyed** (worktree deleted, branch empty — engineer's untracked file never committed; only artifact text in pp db survives).
- Cycle 2 (wf 955c8575): smoke **pass** (harness ran pytest), judge pass → `pass_unlanded`: merge-back failed on `__pycache__` byproduct collision (**MU13**: harness auto-commits unignored build byproducts into the merge branch; smoke run pollutes main's working tree). Honest downgrade behavior (e0c3516) verified.
- Cycle 3 (wf 0537eeef): **complete / smoke pass / merged sha 9042f83b** — file landed in mc-test main, worktree cleaned, $0.28 charged, cursor terminal. Full attended lifecycle proven: plan → step (worktree) → engineer subagent → submit → judge subagent → submit → smoke → merge → charge.

**3B detached (run_wjf3gp9E5s5J, wf f25ba694): verified.** Live best-of-3 on the MU7 fix goal: all 3 candidates bounded at the 600s headless cap, honest fail classification, candidate progression, stage surfaced, no hang (456ca3c/45dc757 field-validated). → MU14 (timeouts record $0 cost). Task too large for headless one-shot → re-done attended.

**MU7 fix landed (commit 096f700).** Attended run dc842fd5/run_f_xUsd4p5vSO: engineer implemented + 25 targeted tests green in worktree; judge PASS (rigorous line-verified critique); smoke failed with the 5F/4S phantom signature → surfaced → **worktree+branch destroyed (MU12-b)**; recovered from the judge artifact (salvage+verify+commit precedent), full suite green **1321 passed** incl. 3 new MU7 regressions; **live-validated: stuck wf b6314c18 resumed synthesis → judge_synthesis → phase "done"** — first standard workflow to complete via operator resume since 2026-06-05. MU8 fix committed as 459129e.
- MU6-rc UPDATE: env theory ELIMINATED — full suite GREEN under the server's exact minimal 14-var env (1318/168s). The 5F/4S signature correlates with SLOW runs (568-590s vs ~170s), i.e. load/timing-sensitive tests; names unrecoverable due to MU6b tail-only retention. Reclassified S2 (attended finalize on Hydra repo is unreliable under load, not deterministically broken).

**3C budget tripwire (wf a10c3f13): PASS.** $0.20 charged against $0.10 cap → finalize returned `budget_downgrade:true` + `budget_block:true`, stage honestly failed/surfaced, nothing merged.

**3D replay: PASS** (dry reconstruct, new wf id, no LLM calls). **3E adapters: PASS** — executive (agent-impersonation): detached resume honestly `deferred_to_host` → step → `ceo` host_action → submit → **complete** ($0.12); garland (claude-skill): shim resolved `brand-strategist` → submit → **complete** ($0.05).

### Tier 4 — Expensive cells
**4A multi-squad campaign (wf 9b7b835e, budget $5): PASS with one integration finding.** 3-squad plan (executive+garland+engineering), approval gate → detached resume: engineering ran HEADLESS LIVE codegen with real cost accounting ($1.87 charged) but surfaced; exec+garland honestly deferred_to_host. Host-driven completion: step re-opened the surfaced engineering task (retry semantics ✓) → engineer+judge → **complete, merged sha f8e1443, smoke pass**; executive (ceo) + garland (brand-strategist) legs completed via squad seam ($0.10/$0.05). MU7 fix then resumed the campaign through synthesis (**DECISION_RECORD synthesis_merge event fired**) + judge_synthesis → postcheck honestly parked `surfaced` → **MU15**. Total spend $2.28/5.00, every leg individually charged.
**4B fleet (wf 60775c00, budget $4, repos mc-test + mu-fleet-scratch): core machinery PASS.** Equal split (repo_budgets $2/$2 exact), isolated per-repo spend ledgers (mc-test $2.63, mu-fleet-scratch $2.02), both legs returned DECISION_RECORDs, deterministic close. Neither headless leg landed its file (consistent with the day's pattern: headless codegen generates but rarely finalizes-complete; attended lands reliably). → MU16 (repo budget not enforced mid-leg; 116% total overspend).
**4C best-of-N: covered by live evidence, no extra spend** — two real best-of-3 runs today (run_fTRQTRWKyi_- and run_wjf3gp9E5s5J) exercised candidate generation, judge_order shuffle, per-candidate verdicts, Borda, honest all-fail stage outcomes.
**4D replay --live: PASS** — replay of ca50a4f9 minted new wf 7c3c8c1c, live dispatcher, re-ran intake→planner, paused at approval with gates intact (rejected in cleanup to avoid duplicate spend).

## Continuation round (2026-07-05 evening): MU12 + MU6-rc FIXED

- **MU12 fixed (commit 0cfdb6c):** `_preserve_non_complete_work` in host_bridge.py commits engineer changes to the attended branch before teardown on non-complete outcomes (fail-soft, `attended.preserved`/`attended.preserve_failed` events); pass_unlanded path advertises the branch committed by `_merge_worktree_back`; cursor + submit result carry `preserved_branch`; 3 MU12 regression tests. Full attended pipeline used: engineer → **cross-vendor codex judge (gpt-5.4) returned REVISE** (caught the missing pass_unlanded case) → Reflexion retry → codex PASS 0.95/0.99/0.90. The pre-fix teardown destroyed the worktree one last time (smoke flake); salvaged from the judge artifact + engineer transcript. Suite 1324 green.
- **MU6-rc ROOT CAUSE FOUND & FIXED (commit c910e22):** definitive repro — running the suite from a git WORKTREE produces exactly the phantom `5 failed / 4 skipped`: all 5 are repo-registry tests (`test_repo_targeting.py::test_valid_repo_id_resolves` +3 siblings, `test_repo_registry_extensible.py::test_malformed_extra_config_is_failsoft`) because `_get_base()` anchored on `__file__` resolves the sibling-repo base to `.harness/worktrees/`. Since the attended/best-of smoke ALWAYS runs from a worktree, every engineering finalize on the Hydra repo was doomed (and pre-MU12, judge-passed work was then destroyed — the full defect chain behind "hydra is broken"). Fix: `git rev-parse --git-common-dir` probe (cached, fail-soft, env override intact) anchors on the main repo root. **Self-proving finalize:** the fix's own attended run produced the first green worktree smoke (`smoke_status: pass`) and the MU12 fix carried it through a legitimate merge conflict (`preserved_branch` surfaced in the result); merged manually with a combined-test-blocks conflict resolution.
- Judge-agent tool-grant gap noted: the typed `judge-cross-vendor` subagent attempted Bash (not granted) and stalled twice; host executed `pp_codex.critique` directly with the same inputs (S3, open).
- Cleanup: mu6-repro worktree/branch removed, both fix workflows parked surfaced, attended branches deleted post-merge.

## Continuation round 2: MU15 FIXED — first full lifecycle "done"

- **MU15 fixed (merged autonomously, sha f8f45be):** the tasks channel is an `_append` reducer (in-place status update impossible), so the fix adds `attended_done_task_ids` on HydraState (strict complete-only subset — surfaced/aborted attended outcomes stay out so governance is NOT weakened), written by `_cmd_attended_submit` on complete cursors and consulted by `enforce_governance` to skip the surfaced/deferred_to_host checks. Judge verified the failed-task check runs BEFORE the filter (no bypass), no injection vector, dispatcher skip-logic unaffected. 3 MU15 regression tests incl. an end-to-end real-graph resume test.
- **Pipeline milestone:** the MU15 run itself was the first attended run on the Hydra repo to go plan → engineer → judge → smoke PASS (worktree, thanks MU6-rc) → **merge autonomously** — no salvage, no manual conflict work.
- **Lifecycle milestone (wf ae8837ef):** first Hydra workflow ever to complete the FULL lifecycle with real engineering work: attended complete+merge (mc-test sha 9f0b5c9) → resume → synthesis → judge_synthesis → postcheck → **phase "done"**. Cosmetic residual: the task-status display column still reads "surfaced" (append-reducer); status rendering could join against attended_done_task_ids (S3).

## Final summary (campaign complete, 2026-07-05)

**Verdict: Hydra's core engine is sound; the "broken" experience came from four independent defects, three now fixed and verified, plus a set of reliability gaps around the attended/detached finalize paths — all catalogued below.**

Fixed this session:
- **MU2** — removed leaked User-level `HYDRA_PP_STAGE_ACTIVE=1` (enforcement hooks live again; re-verify in a fresh session).
- **MU8** (commit `459129e`) — attended MCP surface un-frozen (`stdin=DEVNULL`); plan/step/submit now respond in ~2s.
- **MU7** (commit `096f700`) — bare synthesis/judge_synthesis interrupts resumable; live-validated (wf b6314c18 → "done"); +3 regression tests; suite 1321 green.

Verified working end-to-end: attended engineering (plan→step→engineer→judge→smoke→merge→charge; merged shas 9042f83 + f8e1443 in mc-test), detached launch reliability layer (bounded 600s gens, honest classification, no hangs), budget tripwires (80% downgrade + 100% block), HITL approve/reject incl. bulk bare-interrupt reject, replay dry + live, executive + garland adapters via the host-executor seam, 3-squad campaign with DECISION_RECORD synthesis merge, fleet split/isolation/deterministic close, routing (with MU9a/9b caveats), envelope validation (fail-closed; MU10 caveat), governance surfaces (constitution sha stable throughout, venom fail-open by design, capability run-A degraded-warn posture).

Top open items by priority:
1. **MU12/MU12-b (S1-impact)** — commit worktree to the attended branch BEFORE teardown on non-complete outcomes (judge-passed work must never be destroyed).
2. **MU15 (S2)** — reflect attended completions into the graph task ledger so finished campaigns close "done", not "surfaced".
3. **MU6/MU6-rc (S2)** — smoke gate: full-suite-as-smoke is slow (568s under contention vs 170s idle), flaky 5F/4S under load, 600s cap ≈ baseline duration; add scoped smoke profile + full-output retention (MU6b/MU8b).
4. **MU10 (S2)** — envelope_record cannot record any typed envelope (breaks AgentSmith bridge writes).
5. **MU16 (S2)** — enforce per-repo fleet budgets mid-leg; **MU14 (S3)** — charge timed-out gens.
6. **MU1/MU3/MU4/MU5/MU9a/MU9b/MU11/MU13** — diagnostics, deprecation, hygiene, routing quality (see ledger).

Spend: ≈ $13-14 total (fleet $4.65, campaign $2.28, MU7-fix runs ~$2.9 incl. detached best-of timeouts at unrecorded cost, attended probes ~$1.1, 3C/3E ~$0.5, plus the morning's pre-audit $3.72 diagnosed as MU5).

## MU workflow ids (audit trail)

Probes: b6314c18 (keystone smoke → MU7 live validation, **done**), 795aa5f9/0a7887a9/ce4d31b8 (routing, rejected), a2f95c8b (attended seam probe, surfaced), 4c380c71/02f81593/75d45ca1/6cf5598c/c12a877c/795025e4/6028e782 (MU8 timeout probes, no state), a70fd251/955c8575/0537eeef (3A cycles; 0537eeef **complete/merged**), f25ba694 (MU7 detached attempt, rejected), dc842fd5 (MU7 attended fix run, surfaced honestly; fix salvaged → commit 096f700), a10c3f13 (3C tripwire, budget-blocked, rejected), 19a1f982 (3E executive, **complete**), ca50a4f9 (3E garland, **complete**), 893b3383 (3D replay dry), 7c3c8c1c (4D replay live, rejected), 9b7b835e (4A campaign, surfaced per MU15, all legs complete, merged sha f8e1443), 60775c00 (4B fleet, surfaced).

Scratch repos: mc-test (git-initialized + smoke target this session), mu-fleet-scratch (created + registered in ~/.hydra/repos.json). All attended probe branches deleted; no worktrees left behind.
